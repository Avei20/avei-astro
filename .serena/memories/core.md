# Core

- Astro site root: `avei-astro/`; current public URL set in `astro.config.mjs` as `https://avei.ovh`.
- Source map: pages in `src/pages`, layouts in `src/layouts`, components in `src/components`, content collections in `src/content`, styles in `src/styles`.
- Main route is `src/pages/index.astro`; existing sections also include `blog`, `chat`, `graph` routes.
- Read `mem:tech_stack` for runtime/framework pins and integration quirks.
- Read `mem:conventions` before editing app structure or Vuetify/Astro integration.
- Read `mem:suggested_commands` for dev/build commands.
- Read `mem:task_completion` before final validation.