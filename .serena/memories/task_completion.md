# Task Completion

- For Astro/page/style changes: run `bun build` from `avei-astro/`.
- If dev server needed for visual/manual check: start with `astro dev --background`, inspect, then stop with `astro dev stop`.
- No dedicated lint/test script exists in `package.json`; do not claim lint/test validation unless added or run through another explicit tool.